package main

func main() {

	// s := "how do you do"
	// numMap := make(map[string]int, 5)
	// sList := strings.Split(s, " ")

	// for _, c := range sList {
	// 	numMap[c] += 1
	// }
	// fmt.Println(numMap)

}
